spring.application.name=ecommerce-demo

# Configuration for every-nth order discount generation
ecommerce.coupon.everyNth=1    

# Server port (optional override)
# server.port=8080

# Database placeholder (currently unused)
# spring.datasource.url=jdbc:h2:mem:testdb

# Temporarily disable devtools automatic restart to avoid classloader mapping issues
#spring.devtools.restart.enabled=false
